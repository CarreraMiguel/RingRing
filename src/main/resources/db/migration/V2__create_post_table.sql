CREATE TABLE IF NOT EXISTS POSTS
(
    Post_Id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 0) NOT NULL,
    title VARCHAR (80),
    body VARCHAR (1500),
    createdAt TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES USERS(Id)
);